name: Setup nix
description: Installs a specified version of nix
runs:
  using: composite
  steps:
  - name: Install nix
    uses: DeterminateSystems/nix-installer-action@v16
    with:
      # Commit corresponds to tag v3.0.0 and installs nix v2.26.2.
      source-revision: 560156e95edfb29e458c5ac5f8703dda6d1069b9
      # Disable telemetry.
      diagnostic-endpoint: ""
